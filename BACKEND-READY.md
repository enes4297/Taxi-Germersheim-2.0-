# Backend-Ready Notes

- Diese Demo-Architektur bleibt vorerst auf LocalStorage angewiesen.
- Die neue zentrale Datenzugriffsschicht liegt in admin/taxi-data-service.js.
- Mitarbeiter, Fahrzeuge, Schichten, Pläne und Benachrichtigungen werden über diese Schicht vorbereitet.
- Noch keine echten Backend-URLs, API-Keys oder Secrets sind eingebaut.
- Die bestehende UI-Logik bleibt erhalten; nur die Datenzugriffsweise wird konsolidiert.
